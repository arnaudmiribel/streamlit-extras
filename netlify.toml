[build]
publish = "site"
command = "python3 -m pip install --upgrade pip && python3 -m pip install -r requirements.txt && python3 -m pip install uv && uv sync && uv pip install -r docs/requirements.txt && uv run mkdocs build -d site -v"
